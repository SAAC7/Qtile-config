# Mata cualquier instancia previa
pkill ssh-agent 2>/dev/null

# Arranca ssh-agent y guarda el socket en una ruta fija
ssh-agent -a "$XDG_RUNTIME_DIR/ssh-agent.socket" -D &
export SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/ssh-agent.socket"
